@model IEnumerable<TBL_TRN_REGISTRATION>


@{ 
    if (Model?.Count() > 0)
    {
        //var list = RP.GetData();
        //list.Count();
<table class="table table-bordered table-light table-striped">
    <tr>
       @*@Html.DisplayNameFor(model => model.First().Id)*@
        <th><label asp-for="First().USERID"> </label></th>
        <th><label asp-for="First().USERNAME"> </label></th>
        <th><label asp-for="First().PASSWORD"> </label></th>
      
    </tr>

    @foreach (TBL_TRN_REGISTRATION item in Model)
    {
        <tr>
            <td><label> @item.USERID</label></td>
            <td><label> @item.USERNAME</label></td>
            <td><label> @item.PASSWORD</label></td>
    
        </tr>
    }


</table>

}

        }
